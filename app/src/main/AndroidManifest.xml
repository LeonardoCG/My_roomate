<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my_roomate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Buzon"/>
        <activity android:name=".BuzonAdapter" />
        <activity
            android:name=".PopupError"
            android:theme="@style/AppTheme2.TemaPopup" />
        <activity
            android:name=".PopupCambiada"
            android:theme="@style/AppTheme2.TemaPopup" />
        <activity android:name=".restablecercontrasenia" />
        <activity android:name=".Login_Activity" />
        <activity
            android:name=".menu_nav"
            android:label="@string/title_activity_menu_nav" />
        <activity
            android:name=".registro"
            android:theme="@style/AppTheme.Launcher" />
        <activity android:name=".CheckCodiContainer" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>